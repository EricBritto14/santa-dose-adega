
export type Product = {
  idProduto: number | null,
  tipo: string,
  nome: string,
  tamanho: string,
  data_cadastro: string | null,
  quantidade: number,
  valor: number,
  data_validade: string,
}